# MANIFEST.md — inventory of `runs/`

`runs/` holds raw generation data and is **gitignored**. Every directory created under it must
be listed here with: what produced it, the exact command, and its size. This file is committed;
the data is not.

| run dir | packet | produced by | size | notes |
|---|---|---|---|---|
| `smoke/` | W0b | `python src/smoke_vllm.py` and `python src/smoke_hooks.py` on pod `gwhn0ex0eeyntn`, rsync'd back | 60 KB | 2 files: `smoke_vllm_qwen3-8b.json` (F-012), `smoke_hooks_qwen3-8b.json` (F-013). **Committed by exception** — they are 60 KB, they are this packet's only experimental artefact, and F-012/F-013 cite them as evidence. Bulk rollout data from W2 on stays gitignored. |
| `w1_neutral/` | W1 | `python src/gen_neutral.py --model <hf-id>` on pod `gwhn0ex0eeyntn`, rsync'd back | 3.2 MB | 4 dirs x `neutral.json`, 50 baseline rollouts each. **Committed by exception** (3.2 MB, and P-001/P-002/D-011 are computed from them; `src/tau.py` must be re-runnable by the auditor). |
| `w2_screen/` | W2 | `python src/gen_mirrored.py --model <hf-id> --tau <τ>` on pod `axvdenxbcepd10`, rsync'd back | 3.7 MB | 2 dirs x {`below_good.json`,`above_good.json`}, 50 incentive rollouts per condition. **Committed by exception** (3.7 MB, and P-003 / G-001 / D-016 / V-003 are computed from them; `src/landing_gap.py` and `src/recount_w2.py` must be re-runnable by the auditor). |
| `w3_frozen/` | W3 | `python src/gen_w3.py --tau-a 15300000 [--tau-b 4500000000] --arms ...` on pod `bkl3m9ieis977o`, rsync'd back | 2.5 MB | `form_A/{below,above}_good.json` (150 each) + `form_B/{baseline,below_good,above_good}.json` (50/150/150). **The FROZEN DATASET** (PR-003 item 8): no resampling from these arms. **Committed by exception** — P-004/P-005/G-002/V-005 are computed from them and `src/behaviour_w3.py`, `src/direction_judge.py` and `src/recount_w3.py` must be re-runnable by the auditor. |
| `w4_acts/` | W4 | `python src/replay_w4.py` on pod `io6c1fhnarzoj9`, rsync'd back | **1.4 GB of 3.28 GB — INCOMPLETE** | 6 x `w4_acts_<form>_<arm>.safetensors`, fp16 `acts` tensor `[n_points, 48, 5120]` (post-block residual stream, all 48 decoder layers). **NOT committed** (too large; `runs/` is gitignored). The row index is `analysis/out/w4_positions/w4_positions_<form>_<arm>.json`, which IS committed and carries every position's trace id, kind, token index and span token ids. **Three arms are complete and integrity-verified** (rows == index `n_points`): `A_above_good` 558,858,544 B `sha256 abcde06b…`, `A_neutral` 133,693,736 B `sha256 14715479…`, `B_baseline` 203,981,096 B `sha256 f380fc05…`. **Three are truncated prefixes** because RunPod stopped the pod mid-rsync when the account balance went negative (D-020): `A_below_good` 170,754,048 of 585,892,144 B, `B_above_good` 181,272,576 of 881,295,664 B, `B_below_good` 216,236,032 of 913,735,984 B. Do not read the truncated three; regenerate them with `python src/replay_w4.py` on a funded pod. **Superseded — see the W5 note below: these files no longer exist.** |
| `w5_subsample/` | W5 | `python src/direction_w5.py` then `python src/w5_cell.py` on pod `io6c1fhnarzoj9`, rsync'd back in 4 parallel streams | 588 MB | **NOT committed** (`runs/` is gitignored). Two files of activations plus their JSON indexes, and the only activation data that survives the pod. (1) `w5_subsample.safetensors` — the PR-004 item-7 10 % subsample: **every 10th point of the global point index**, `[667, 48, 5120]` fp16, 327,844,104 B, `sha256 0e4b036a…`; index `w5_subsample_index.json` `sha256 f744ed54…`. (2) `w5_cell.safetensors` — the v_p̂ **analysis cell**: every `est` point of both `above_good` arms, `[528, 48, 5120]` fp16, 259,522,800 B, `sha256 55fd0a8d…`; index `w5_cell_index.json` `sha256 a7bb463b…`. Shipped as JC-4 after the frozen analysis completed, because the 10 % rule lands only 17 of 366 labelled cell points and cannot support PR-004's recount (D-024). `python3 src/w5_recount.py` rebuilds v_p̂(ℓ\*) from (2) alone. |

| `w7_steer/` + `w7_smoke_pod/` | W7 | `python src/steer_w7.py --out-root runs/w7_steer` on pod `heenrekmx8f4da`, rsync'd back | 6.1 MB | 23 arm files, `<arm>.json`, 50 steered generations each (1,150 total), PR-005 item 3's arm table. Each file carries the full injection record: layer, α, direction kind, null seed, ‖Δμ‖, ‖injected vector‖, the v_p̂^B sha256, the exact prompt and templated prompt, the seed block per batch, and every generation's raw text, finish_reason, token count and distinct-4-gram ratio. **Committed by exception** — P-008/V-012 are computed from them and `src/w7_recount.py`, `src/analyze_w7.py` and `src/samples_w7.py` must be re-runnable by the auditor. Also holds `w7_gen.log` (the generation log of record) and, in `w7_smoke_pod/` (56 KB), the two pre-run GPU sanity arms — shipped so V-012(5)'s "nothing unique pod-side" attestation is literally true. |

| `w7b_steer/` + `w7b_smoke_pod/` | W7b | `python src/steer_w7b.py --out-root runs/w7b_steer` on pod `u3g0qm180kvqnd`, rsync'd back | 3.2 MB + 64 KB | 12 arm files, `<arm>.json`, 50 low-dose steered generations each (600 total), PR-006 item 2's arm table: v_p̂ at α ∈ {+0.5, +0.25, −0.25, −0.5} and four NEW random directions (seeds 9011–9014) at α = ±0.5, all L27 `above_good`. Each file carries the full injection record (layer, α, direction kind, null seed, ‖Δμ‖, ‖injected vector‖, the v_p̂^B sha256, the exact and templated prompt, the per-batch seed block, and every generation's raw text, finish_reason, token count and distinct-4-gram ratio) and is stamped `packet: W7b` / `pre_registration: PR-006`. **No sham arm here — W7's `w7_steer/B_above_sham.json` is REUSED as the α=0 reference (PR-006 item 2, declared).** **Committed by exception** — P-009/V-014 are computed from them and `src/w7b_recount.py`, `src/analyze_w7b.py`, `src/samples_w7b.py` and `src/w7b_floor.py` must be re-runnable by the auditor. Also holds `w7b_gen.log` (the generation log of record) and, in `w7b_smoke_pod/` (64 KB), the two pre-run GPU sanity arms plus the pod-side provisioning scripts and log (`prep.sh`, `prep.log`, `run7b.sh`) — shipped so V-014(5)'s "nothing unique pod-side" attestation is literally true. |

**W5 note — `w4_acts/` no longer exists anywhere.** Pod `io6c1fhnarzoj9` was terminated at
2026-08-30 05:28:18 UTC per R-010(2), and with it the 3.28 GB of W4 activation tensors. They
are **regenerable**: `python src/replay_w4.py` on a funded pod reproduces all six arms in
94.8 s of forward passes (~$0.12), and W5 verified before termination that the six pod-side
files matched their indexes row-for-row with 30/30 spans decoding exactly
(`python src/w5_integrity.py`, V-010). The three truncated laptop prefixes from D-020 were
deleted; the three complete laptop copies (`A_above_good`, `A_neutral`, `B_baseline`) are also
gone, since keeping half a dataset invites reading it. What remains on the laptop is
`w5_subsample/` above, which is what R-010(3) rules the laptop should hold.

**W4 note.** `w4_acts/` is the first run directory that is NOT committed by exception: at 3.28 GB it is three orders of magnitude past the previous largest. Everything the ledger cites about it — point counts, kinds, token indexes, the decode acceptance check — is computed from the committed positions indexes, not from the tensors.

| `w11_clarified/` | W11 | `python src/gen_w11.py --n 150` on pod `t1mm1e0l3f7fuh`, rsync'd back | 2.8 MB | `form_A/{below,above}_good.json` + `form_B/{below,above}_good.json`, **150 clarified rollouts each (600 total)**, PR-007 item 2's arms, seeds 9814–10413. Each file is stamped `packet: W11` / `clarified: true` and carries **both** `prompt_text` (the clarified prompt) and `w3_prompt_text` (W3's, verbatim), so the one-sentence manipulation that defines this packet is inside the data and not only in the code. **Committed by exception** — P-010/V-017 are computed from them and `src/w11_recount.py`, `src/analyze_w11.py`, `src/w11_cells.py` and `src/samples_w11.py` must be re-runnable by the auditor. Also holds `w11_gen.log`, the generation log of record. **No pod-side smoke directory:** W11's smoke (`gen_w11.py --selftest`, 24 clauses) is laptop-only and needs no GPU, so there is nothing pod-unique to ship. |

| `w14_degraded/` | W14 | `python src/gen_w14.py --n 150` on pod `ysuxh20tiu5dvg`, rsync'd back | 1.6 MB | `form_B/{below,above}_good.json`, **150 degraded rollouts each (300 total)**, PR-010 item 2's arms, seeds 10414–10713, **0 truncated**. Each file is stamped `packet: W14` / `degraded: true` and carries **all three wordings of this dose-response** — `prompt_text` (degraded), `w3_prompt_text` (W3's, verbatim), `w11_prompt_text` (W11's clarified, verbatim) — plus `replaced_substring` and `replacement`, the exact swap PR-010 item 1 froze. The manipulation that defines this packet is inside the data, not only in the code. **Committed by exception** — P-013/V-024 are computed from them and `src/w14_recount.py`, `src/analyze_w14.py`, `src/w14_dircheck.py` and `src/samples_w14.py` must be re-runnable by the auditor. Also holds `w14_gen.log`, the generation log of record, which contains **both** launches: the D-050 failure (`FileNotFoundError: 'ninja'`) is not in it — that was attempt 1, whose log is not shipped because it produced no data — and the successful relaunch is. **No pod-side smoke directory:** W14's smoke (`gen_w14.py --selftest`, 30 clauses) needs no GPU and was run identically on the laptop and on the pod; there is nothing pod-unique to ship. |

| `w15_harvest/` | W15 | `python src/gen_w15.py --n 1000 --hf` on pod `pu0m8r6jtng3ks`, rsync'd back | 5.2 MB | `form_B/above_good.json`, **1,000 natural-wording rollouts**, PR-011 item 1's harvest, seeds **10714–11713**, **0 truncated**, median 407 output tokens. Stamped `packet: W15` / `pre_registration: PR-011` / `wording: natural (W3 form B, unmodified)` and carries **all three wordings** — `prompt_text` (W3's natural, verbatim), `w11_prompt_text`, `w14_prompt_text` — so the fact that this packet went back to the *unmodified* prompt is inside the data. `backend` records **D-054's fallback** in the file itself. **Committed by exception** — P-014/V-026 are computed from it and `src/harvest_w15.py`, `src/analyze_w15.py`, `src/w15_recount.py` and `src/samples_w15.py` must be re-runnable by the auditor. Holds **two** logs: `w15_gen.log`, the generation log of record, and `w15_gen_vllm_failed_D-054.log`, the vLLM launch that died on the driver mismatch — **shipped, unlike W14's D-050 failure, because D-054 is a claim about the machine and the traceback is its evidence.** |
| `w15_transplant/` | W15 | `python src/transplant_w15.py --transplant --directions primary mirror` on pod `pu0m8r6jtng3ks` | 2.2 MB | `{primary,mirror}.json`, **40 pairs × 4 arms × 2 directions = 320 generations**, PR-011 item 2. Each row carries the teacher-forced `prefix_text`, the arm's `continuation_text`, the `reconstructed` trace the statistic is computed on, `continuation_token_ids`, the recorded L27 projection at **every** decode position (`proj_decode`), `n_edited_positions` and the per-arm `edit_norm_{mean,max}` — so the intervention's magnitude and its effect on the belief coordinate are both auditable from the data alone. Also `w15_transplant.log`. |
| `w15_transplant_deep/` | W15 | `python src/transplant_w15.py --transplant --directions primary --cut-frac 0.40` | 2.2 MB | `primary.json`, **40 pairs × 4 arms = 160 generations** — the **DECLARED POST-FREEZE JC-4 DIAGNOSTIC**, same pairs/arms/seeds/edit at a 40 %-of-`n_gen` cut. `cut_rule` names it as post-freeze **inside the file**. It bounds P-014's null and **is never mixed into the frozen statistic**; `src/w15_deep.py` is the only script that reads it. Also `w15_deep.log`. |
